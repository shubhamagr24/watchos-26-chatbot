services:
  - type: web
    name: backend-service
    runtime: python
    buildCommand: "uv install"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    plan: free
    publish:
      directory: ./